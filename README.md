# label_checker
small tool made with tkinter for checking auto generated LabelMe labels

## running 
 

```bash
git clone *this repo*
cd *this repo*
mkdir env 
python3 -m venv env
pip3 install -r req.txt
python3 main.py
```

